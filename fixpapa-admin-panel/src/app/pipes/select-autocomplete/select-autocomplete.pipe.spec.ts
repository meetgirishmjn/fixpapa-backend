import { SelectAutocompletePipe } from './select-autocomplete.pipe';

describe('SelectAutocompletePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectAutocompletePipe();
    expect(pipe).toBeTruthy();
  });
});
