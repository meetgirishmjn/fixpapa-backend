import { FilterEngineerPipe } from './filter-engineer.pipe';

describe('FilterEngineerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEngineerPipe();
    expect(pipe).toBeTruthy();
  });
});
