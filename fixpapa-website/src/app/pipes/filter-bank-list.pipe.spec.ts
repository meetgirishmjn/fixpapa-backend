import { FilterBankListPipe } from './filter-bank-list.pipe';

describe('FilterBankListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBankListPipe();
    expect(pipe).toBeTruthy();
  });
});
