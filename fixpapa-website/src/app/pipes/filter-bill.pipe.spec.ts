import { FilterBillPipe } from './filter-bill.pipe';

describe('FilterBillPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBillPipe();
    expect(pipe).toBeTruthy();
  });
});
