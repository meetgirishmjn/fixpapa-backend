// let first = this.loginForm.value.emailOrMobile.substring(0,1);
				// let length = val.length;
				// if(first === '0'){
					// for(var i = 0;i<=val.length-1;i++){
						// if(val[i] === '0'){
							// val = val.substring(1);
							// i=0;
						// }
					// }
					//val = this.loginForm.value.emailOrMobile.substring(1);
				// }
				// length = val.length;
				// if(length == 10){
					// mobile = val;
				// }
				// else if(length > 10){
					// if(length >= 13){
						// let toCheck = this.loginForm.value.emailOrMobile.substring(0,3);
						// if(toCheck === '+91'){
							// let newVal = this.loginForm.value.emailOrMobile.substring(3,14);
							// if(newVal[0] !== '0'){
								// mobile = this.loginForm.value.emailOrMobile.substring(3,14);
								// mobile = '91' + mobile; 
							// }else{
								// mobile = this.loginForm.value.emailOrMobile.substring(4,14);
								// mobile = '91' + mobile; 
								// this.invalidMobOrEmail = true;
							// }
						// }else{
							// this.invalidMobOrEmail = true;
						// }
					// }
					// else if(length === 12){
						// let toCheck = this.loginForm.value.emailOrMobile.substring(0,2);
						// if(toCheck === '91'){
							// let newVal = this.loginForm.value.emailOrMobile.substring(3,14);
							// if(newVal[0] !== '0'){
								// mobile = this.loginForm.value.emailOrMobile;
							// }else{
								// this.invalidMobOrEmail = true;
							// }
						// }
						// else{
							// this.invalidMobOrEmail = true;
						// }
					// }
				// }
				// else if(length < 10){
					// this.invalidMobOrEmail = true;
				// }